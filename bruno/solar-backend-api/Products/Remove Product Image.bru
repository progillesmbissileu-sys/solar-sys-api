meta {
  name: Remove Product Image
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/api/product/media/{{productId}}/images
  body: json
  auth: inherit
}

body:json {
  {
    "imageId": "{{imageId}}"
  }
}
