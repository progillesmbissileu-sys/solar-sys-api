meta {
  name: All Products
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/product?sort[created_at]=desc
  body: none
  auth: inherit
}

query {
  sort[created_at]: desc
  ~sort[price]: desc
  ~q: BIS
  ~sort[designation]: desc
}
