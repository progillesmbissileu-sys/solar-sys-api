meta {
  name: Grouped By Categories
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/product?sort[created_at]=descr&sort[price]=desc
  body: none
  auth: inherit
}

query {
  sort[created_at]: descr
  sort[price]: desc
  ~q: BIS
  ~sort[designation]: desc
}
